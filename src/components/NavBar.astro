---
const navItems = [
  {
    name: "Home",
    href: "/",
  },
  {
    name: "Blog",
    href: "/blog",
  },
  {
    name: "Projects",
    href: "/projects",
  },
];
---

<nav aria-label="Primary">
  {
    navItems.map((navItem) => (
      <a
        aria-label={navItem.name}
        href={navItem.href}
        class="mr-3 text-cMaroon  decoration-dotted underline-offset-2 hover:underline"
      >
        {navItem.name}
      </a>
    ))
  }
</nav>
